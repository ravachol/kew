name: Deploy to Codeberg Pages
on:
  push:
    branches: [main]
    paths: ['docs/**', 'website/**']

jobs:
  build-and-deploy:
    runs-on: codeberg-tiny
    container:
      image: alpine:edge
    steps:
      - name: Checkout
        uses: https://code.forgejo.org/actions/checkout@v4

      - name: Install build tools
        run: apk add --no-cache make sblg lowdown

      - name: Build site
        working-directory: website
        run: make install PAGES_SRC_DIR=site_src

      - name: Deploy to Codeberg Pages
        uses: https://codeberg.org/git-pages/action@v2
        with:
          site: 'https://${{ forge.repository_owner }}.codeberg.page/ravachol/'
          token: ${{ forge.token }}
          source: website/site/
